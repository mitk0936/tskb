// @ts-check

// Root-level ignores only. Each folder owns its own eslint.config.js.
export default [
  {
    ignores: ["**/dist/**", "**/node_modules/**", "**/*.d.ts", "references/**", "tests/**"],
  },
];
