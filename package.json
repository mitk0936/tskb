{
  "name": "tskb-workspace",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "postinstall": "npm run build",
    "build": "npm run build --workspaces --if-present && npm run build:docs",
    "build:docs": "tskb \"./docs/**/*.tskb.tsx\" --tsconfig ./docs/tsconfig.json",
    "clean": "npx rimraf --glob node_modules **/node_modules **/dist **/.tskb",
    "test": "vitest run",
    "test:coverage": "c8 --src packages/tskb/src --include \"packages/tskb/dist/**\" --reporter text --reporter html -o coverage vitest run",
    "test:watch": "vitest",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md,yml,yaml}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,jsx,json,md,yml,yaml}\"",
    "prepare": "husky"
  },
  "dependencies": {
    "tskb": "workspace:*"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.1",
    "c8": "^11.0.0",
    "husky": "^9.1.7",
    "mkdirp": "^3.0.1",
    "prettier": "^3.2.4",
    "rimraf": "^6.0.1",
    "vitest": "^4.1.1"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "keywords": [],
  "author": "Dimitar Mihaylov",
  "license": "MIT",
  "description": "Task Knowledge Base - A TypeScript-based system for documenting software architecture using TSX syntax"
}
